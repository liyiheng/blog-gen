# $HOME/.local/share/fcitx5/rime/luna_pinyin.custom.yaml

patch:
  "switches/@2/reset": 1  #表示將 switcher 列表中的第3個元素（即 simplification 開關）的初始值重設爲狀態1（即「汉字」）。

__patch:
# Rx: emoji:customize:schema=luna_pinyin {
  - patch/+:
      __include: emoji_suggestion:/patch
# }
